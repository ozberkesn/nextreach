services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: nextreach
    ports:
      - "5432:5432"
    volumes:
      - nextreach-postgres:/var/lib/postgresql/data

volumes:
  nextreach-postgres:
